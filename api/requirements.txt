# Python dependencies for Vercel serverless functions (Python 3.12)
# NOTE: PyTorch has been removed! Models are now in numpy-only format.
# scipy is needed for exact GELU computation (scipy.special.erf)
flask==3.0.0
flask-cors==4.0.0
numpy>=2.0.0
scipy>=1.11.0
