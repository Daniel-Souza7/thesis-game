[project]
name = "thesis-game-api"
version = "1.0.0"
description = "Optimal Stopping Game API - Numpy-only (no PyTorch)"
requires-python = ">=3.12"
dependencies = [
    "flask==3.0.0",
    "flask-cors==4.0.0",
    "numpy==1.26.0",
]
