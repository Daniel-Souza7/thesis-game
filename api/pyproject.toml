[project]
name = "thesis-game-api"
version = "1.0.0"
description = "Optimal Stopping Game API - Numpy-only (no PyTorch)"
requires-python = ">=3.12"
dependencies = [
    "flask==3.0.0",
    "flask-cors==4.0.0",
    "numpy>=2.0.0",
    "scipy>=1.11.0",
]
